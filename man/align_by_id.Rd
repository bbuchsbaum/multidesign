% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alignment.R
\name{align_by_id}
\alias{align_by_id}
\alias{align_by_id.hyperdesign}
\title{Align Hyperdesign Blocks by Their Entity Contract}
\usage{
align_by_id(x, ...)

\method{align_by_id}{hyperdesign}(x, fill = NA_real_, sort_ids = FALSE, ...)
}
\arguments{
\item{x}{A hyperdesign with declared correspondence and `space = "common"`.}

\item{...}{Additional arguments passed to methods.}

\item{fill}{Length-one numeric value used to display absent rows and
explicitly unobserved cells.}

\item{sort_ids}{Logical; if `TRUE`, use sorted global identifiers rather than
first-seen order.}
}
\value{
An `aligned_hyperdesign` list containing `x`, `cells`, `observed`,
  `correspondence`, `column_design`, and `block_names`.
}
\description{
Places every common-space block on the global entity universe described by
[correspondence()]. The result keeps row presence and cell observation as
separate logical arrays; the fill value is presentation only.
}
\examples{
a <- multidesign(
  matrix(c(NA, 0, 1, 0), ncol = 2, byrow = TRUE),
  data.frame(id = c("A", "B")),
  cells = matrix(c(TRUE, TRUE, TRUE, FALSE), ncol = 2, byrow = TRUE)
)
b <- multidesign(
  matrix(c(0, 1, 1, 1), ncol = 2, byrow = TRUE),
  data.frame(id = c("B", "C"))
)
hd <- hyperdesign(list(a = a, b = b), id = "id", space = "common")
aligned <- align_by_id(hd)
aligned$observed
aligned$cells[, , "a"]
aligned$x[, , "a"]
}
